# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'paymentgateway' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for paymentgateway

  target 'paymentgatewayTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'paymentgatewayUITests' do
    # Pods for testing
  end
     pod 'razorpay-pod'
end
